Be written in Java

Must be a console application

Have persistence --> PostgreSQL database thru Docker
    - Persisted data must include multiple types of objects from your application
    - Persisted data must include one many-to-many relationship
    - If in SQL, data must be normalized to 3NF

Demonstrate separation of concerns
    - Presentation Layer --> console I/O (i.e. numerical menu)
    - Logic Layer --> processes user input, validations (error handling)
    - Data Access Layer --> manages interactions w/ database 

Include at minimum 2 class objects/ models

Application should run until the user decides to exit the application

Handle errors / exceptions gracefully

Save project to a public Git Repo

Application must include unit testing (JUNit, Mockito)
    - At least 10 individual tests
    - Mock the repo layer
    - Should have happy path, negative result tests, and edge cases
    - Apply TDD for one method

Project idea: NBA database and game simulator
    - Tracks teams, players, games, and individual box scores
    - Can be simulate a game between two teams chosen by user
    - Stats are randomly generated for each player and game
        - Displayed to user and stored in respective tables once generated
        - no game logic or user interactions
    - 